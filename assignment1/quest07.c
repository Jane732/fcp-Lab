#include <stdio.h>
int main()
// to calculate area of triangle.
{
    float b,h;
    printf("Enter the base: ");
    scanf("%f",&b);
    printf("Enter height: ");
    scanf("%f",&h);
    printf("Area of triangle is %.2f",0.5*b*h);
}
